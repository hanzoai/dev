cabal-version:      3.0
name:               hanzo-zap
version:            0.6.0.0
synopsis:           Zero-copy Agent Protocol - 1000x faster than MCP/JSON-RPC
description:
    ZAP (Zero-copy Agent Protocol) is an insanely fast binary protocol for AI
    agent tool calls. It eliminates the serialization, parsing, and copying
    overhead of JSON-RPC by reading directly from network buffers.
    .
    Key features:
    .
    * 1000-2000x faster than JSON-RPC for typical agent workloads
    * Zero allocations for message parsing
    * Full MCP (Model Context Protocol) compatibility via gateway
    * 14 tool categories with 167+ typed operations
    * Built-in security policies (approval and sandbox)
    .
    See <https://github.com/hanzoai/zap> for protocol specification.

license:            MIT
license-file:       LICENSE
author:             Hanzo AI
maintainer:         dev@hanzo.ai
copyright:          2025 Hanzo AI Inc.
category:           Network, AI
build-type:         Simple
extra-doc-files:    README.md
homepage:           https://github.com/hanzoai/zap
bug-reports:        https://github.com/hanzoai/zap/issues

source-repository head
    type:     git
    location: https://github.com/hanzoai/zap.git

library
    exposed-modules:
        Hanzo.Zap
        Hanzo.Zap.Types
        Hanzo.Zap.Client
        Hanzo.Zap.Server

    build-depends:
        base        >= 4.14 && < 5,
        aeson       >= 2.0  && < 2.3,
        bytestring  >= 0.10 && < 0.13,
        text        >= 1.2  && < 2.2,
        network     >= 3.1  && < 3.3,
        async       >= 2.2  && < 2.4,
        stm         >= 2.5  && < 2.6,
        containers  >= 0.6  && < 0.8,
        time        >= 1.9  && < 1.15,
        mtl         >= 2.2  && < 2.4,
        vector      >= 0.12 && < 0.14

    hs-source-dirs:   src
    default-language: Haskell2010
    ghc-options:      -Wall -Wcompat -Widentities -Wincomplete-record-updates
                      -Wincomplete-uni-patterns -Wpartial-fields -Wredundant-constraints

test-suite hanzo-zap-test
    type:             exitcode-stdio-1.0
    main-is:          Main.hs
    hs-source-dirs:   test
    build-depends:
        base,
        hanzo-zap,
        aeson,
        bytestring,
        text,
        containers,
        hspec >= 2.7 && < 2.12

    default-language: Haskell2010
    ghc-options:      -Wall -threaded -rtsopts -with-rtsopts=-N
