/**
 * Re-export ZAP types from canonical @zap-proto/zap.
 */
export type {
  SandboxPolicy,
  Tool,
  ToolCall,
  ToolResult,
  ServerInfo,
  ClientInfo,
  Resource,
  Prompt,
  ServerCapabilities,
  ResourceContent,
  Transport,
  LogLevel,
} from '@zap-proto/zap';

export { MessageType, ApprovalPolicy } from '@zap-proto/zap';
