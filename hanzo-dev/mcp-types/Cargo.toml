[package]
name = "hanzo-mcp-types"
version = { workspace = true }
edition = "2024"
description = "MCP (Model Context Protocol) type definitions for Hanzo AI agents"
license = "MIT"
repository = "https://github.com/hanzoai/dev"
homepage = "https://github.com/hanzoai/dev/tree/main/mcp-types"
documentation = "https://docs.rs/hanzo-mcp-types"
readme = "README.md"
authors = ["Hanzo AI <dev@hanzo.ai>"]
categories = ["development-tools", "api-bindings"]
keywords = ["mcp", "protocol", "ai", "agent", "tools"]

[lib]
name = "mcp_types"
path = "src/lib.rs"

[lints]
workspace = true

[dependencies]
upstream-mcp-types = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
