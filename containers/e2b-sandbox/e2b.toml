# This is a config for E2B sandbox template.
# You can use 'template_id' (785n69crgahmz0lkdw9h) or 'template_name (hanzo) from this config to spawn a sandbox:

# Python SDK
# from e2b import Sandbox
# sandbox = Sandbox(template='hanzo')

# JS SDK
# import { Sandbox } from 'e2b'
# const sandbox = await Sandbox.create({ template: 'hanzo' })

dockerfile = "Dockerfile"
template_name = "hanzo"
template_id = "785n69crgahmz0lkdw9h"
